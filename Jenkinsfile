pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh "ls"
                sh "pwd"
                sh "touch test1.txt"
            }
        }
    }
}
